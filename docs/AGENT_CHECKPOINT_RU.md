# AGENT CHECKPOINT

## Локальная контрольная точка hardening 2026-08-28

- Ветка `codex/firmware-log-hardening-20260828` от `28eb7584aaeef0cb66aa3c967bf4a162f49b3d0b` добавляет защиту диагностических URL-логов и AFSK-настройки `Wi-Fi`.
- Полные URL больше не должны попадать в `application.cc`, `assets.cc`, `ota.cc`, `mcp_server.cc` и `protocols/websocket_protocol.cc` через логи, экранные сообщения или ошибки. В диагностику выводится только обезличенное описание через `diagnostic_redaction::RedactUrlForDiagnostics`.
- Activation failure больше не печатает тело ответа сервера; ответ дочитывается для корректного закрытия HTTP, но в журнале остаётся только код статуса и пометка, что тело скрыто.
- AFSK больше не выводит полный decoded text на экран и не печатает SSID/password text. Остаются безопасные длины SSID/password и понятное сообщение о получении данных.
- Локальные статические проверки прошли без доступа к устройству: host test `diagnostic_redaction_host_test.cc`, `./scripts/check_sensitive_logging.py`, `git diff --check`, каноническая сборка `GOSHA_OTA_URL='<owner-only production endpoint>' python3 scripts/release.py gosha-v1 --name gosha-v1`.
- Сборочный результат: `gosha.bin` — `3654368` байт, свободно 11% app-раздела; SHA-256 `94473bb2864e73c0897bf7b5116941508641089691de4f60baef826fbbe245cb`. `merged-binary.bin` SHA-256 `0b43aa4d988427bafbec366b7b666984c8bdaf9b408946a19496994f33ab0187`, ZIP SHA-256 `9084ec24e10e61fe096ae303a8c4f65315d80f8553581484eb3986b5c1e072fb`.
- После первого review wave2 закрыт дополнительный блокер: `self.screen.snapshot` дочитывает upload response body, но больше не пишет его в журнал. Это важно, потому что ответ внешнего upload-сервиса может содержать подписанный URL, query или token. Static guard теперь проверяет и такую схему.
- Это не аппаратный допуск и не release на установку. До замены левой сервы и отдельного допуска остаются запрещены USB/serial, flash, перезагрузка ради теста, motion, `trim`, servo sequence и raw `:8080`.

## Свежая контрольная точка 2026-08-27

- Статический remediation-gate опубликован в Draft PR `#24` как `feature/firmware-orange-eyes @ 7751a3ca326174d217536f6a8de7c09433c3e955`; исходный аудит GPT-5.5 дал `NO-GO` из-за hardcoded временного relay, хвостовых пробелов в новом vendored-компоненте и чувствительных runtime-логов, а перечисленные блокеры закрыты в опубликованном head.
- OTA/config больше не имеет сетевого адреса в Git-default. Production-сборка `gosha-v1` требует owner-only `GOSHA_OTA_URL`, валидирует абсолютный HTTP(S)-URL без встроенных учётных данных и печатает только `CONFIG_OTA_URL=<owner-supplied>`.
- При пустом OTA/config endpoint загрузочный version-check завершается сразу, без длительного цикла повторов. Wi-Fi-пароль и activation payload больше не печатаются.
- Секция `runtime_events` применяется целиком: при её отсутствии или неполных `url`/`token` старый endpoint и token удаляются из NVS. HTTP-клиент больше не печатает полный набор заголовков с `Authorization`.
- `self.otto.stop` больше не ставит `ACTION_HOME`: он останавливает текущую задачу, возобновляет обновление батареи и очищает очередь без запуска нового движения.
- `git diff --check origin/main` чист. Статическая сборка ESP-IDF 5.5.2 с неразрешимым тестовым endpoint `.invalid` прошла без flash: `gosha.bin` — `3652384` байт, свободно 12% app-раздела; ZIP является только доказательством сборки и не предназначен для установки.
- Неиспользуемый upstream symlink `_codeql_detected_source_root` удалён: защищённый workspace-export AI Office запрещает symlink и до удаления завершался `unsupported_git_entry` ещё до запуска reviewer-модели.
- Immutable AI Office review `task-20260827T104756Z-immutable-terminal-firmware-pr-24-gate-at-7751a3c` на фактическом `GPT-5.5 / xhigh` завершился terminal `PASS` без P0/P1/P2. Статический gate закрыт, однако PR остаётся Draft/Open и не сливается до общей последовательной приёмки. Из-за неисправной левой сервы flash, USB/serial, перезагрузка ради теста, motion и trim по-прежнему запрещены.

## Предыдущая контрольная точка 2026-08-25

- Временный сетевой канал принят как рабочий обход: `TEMP_NL_RELAY` временно прокидывает роботу путь к `PRIMARY_PLATFORM_SERVER`. Основной серверный контур остаётся на `PRIMARY_PLATFORM_SERVER`; relay не считается новым production-сервером и должен быть заменён на `FUTURE_PRODUCTION_SERVER` в конце месяца.
- Роли портов фиксируются без IP-адресов, секретов и хардкода в прошивке: `18876` — HTTP-контур панели, OTA и config; `18080` — голосовой `WebSocket` и совместимый `MCP`.
- Новый физический робот подтверждён в живом контуре как `gosha-main`: Android показывает `Гоша Main`, робот разговаривает, голосовой сценарий принят.
- Firmware quality gate для `feature/firmware-orange-eyes @ 07d5f6658b6c70c81626ebcc3fbee930ced94fc6`: Draft PR допустим, но merge и установка пока `NO-GO`.
- Причины `NO-GO`: старая документация называла `self.otto.stop` безопасной проверкой, хотя этот инструмент ставит `ACTION_HOME` и запускает движение; `git diff --check` падает на хвостовых пробелах в vendored-компоненте `firmware/local_components/78__esp-ml307`; существующий `firmware/releases/v2.2.2_gosha-v1.zip` содержит устаревший merged-образ и должен быть пересобран перед публикацией артефакта.
- До замены левой сервы запрещены прошивка, перезагрузка ради проверки, любые движения, `self.otto.set_trim`, servo sequence и живые raw-проверки локального `:8080`.
- Read-only допустимы только безопасные неподвижные команды и наблюдения: `gosha.identity.get`, `self.otto.get_status`, `self.battery.get_level`, `self.otto.get_ip`, экран, звук, микрофон, Wi-Fi, панель, OTA/config и runtime-события без движения.

## Предыдущая контрольная точка 2026-08-24

- Подготовлен локальный read-only ответ `gosha.identity.result` на запрос `gosha.identity.get`; ответ остаётся в локальной WebSocket-сессии и позволяет Android сравнить аппаратный `device_id`.
- Изменение перенесено в актуальную ветку нового робота поверх принятого тёмно-оранжевого профиля; каноническая полная сборка `gosha-v1` прошла. Новый образ на устройство не устанавливался.
- Левый сервопривод нового робота аппаратно неисправен или заклинивает. До замены/безопасного отключения не выполнять прошивку, перезагрузку и команды движения.
- Домашний Wi-Fi был настроен, но прямой маршрут до публичного узла платформы оказался внешним сетевым блокером; на 2026-08-25 он временно обойдён через `TEMP_NL_RELAY`.

## Новейшая аппаратная точка `2026-08-23`

- Новый `ESP32-S3` с `16 MB` flash-памяти и `8 MB PSRAM` полностью скопирован до записи; резервный образ хранится вне Git и допускает полный откат.
- Заводская таблица разделов несовместима с профилем `gosha-v1`, поэтому после резервного копирования выполнена первичная полная установка `merged-binary.bin` из `feature/firmware-orange-eyes @ 80310104e895d02d648364460e82d0c2b31e8ba8`, SHA-256 `7c1d9a52b69acae30a838502218a3751481484e1585a99b2137fae8df3200b70`.
- Запись и отдельный `verify_flash` успешны. Первый запуск `gosha 2.2.2` стабилен: `non-camera`, подстройки всех сервоприводов равны `0`, `LH=GPIO8`, `RH=GPIO12`, локальный `WebSocket` запущен, паник, сторожевого таймера, сбросов из-за просадки питания и цикла перезагрузок нет.
- Перекрёстный тест сервоприводов подтвердил работоспособность левого программного канала и `GPIO8`: исправный правый сервопривод двигался на левом разъёме.
- Физический сервопривод левой руки не запускался даже на рабочем правом канале и реагировал только после ручного подталкивания. Текущий диагноз — неисправность самого сервопривода или его кабеля; это не дефект прошивки и не повод менять `trim`.
- До замены левую серву держать отключённой. Не отправлять команды движения, не подталкивать вал под питанием и не использовать `self.otto.set_trim`, поскольку этот путь запускает дополнительное движение.
- Следующий безопасный шаг — приёмка неподвижных функций нового робота. После замены сервы: проверка без качалки, `Home`, правильная механическая установка и только затем малый тест штатным операторским путём.

## Экранная контрольная точка 2026-07-23

- Рабочее дерево: `<FIRMWARE_ORANGE_EYES_WORKTREE>`.
- Ветка: `feature/firmware-orange-eyes`, база — `af4de9c` из стабильного контура событий прошивки.
- Единый экранный профиль `gosha-v1` использует тёмно-оранжевый `#E06F00` для всех 21 GIF-глаз, Wi-Fi, батареи, беззвучного режима, центрального статуса, уведомлений и резервного значка эмоции.
- Цвет задан одним параметром `GOSHA_UI_ACCENT_COLOR_HEX`, который передаётся и сборщику ресурсов, и коду интерфейса; другие платы не меняются.
- При каждом применении темы плата восстанавливает чистый чёрный фон и удаляет возможное фоновое изображение из ресурсов. Критическое предупреждение низкого заряда остаётся красным.
- Проверены 21 GIF и 692 кадра. Каноническая сборка прошла.
- SHA-256 `gosha.bin`: `6d466b9a2a6299fc1cd73048b30452f051163026b199268fc3e92638d5dd481d`.
- SHA-256 `generated_assets.bin`: `12520722b9a56c0b687d072cb668e2f0ede0260b3a7fdef365abe81015231516`.
- SHA-256 выпуска: `da43c5836f3fdadd595317c311c03538d3155ed3d8194a7513d31fdaaa185682`.
- ИИ-офис `task-20260723T090714Z-read-only-review-firmware-dark-orange-instrument-panel` не нашёл кодовых P0/P1/P2; документационный P2 исправлен. Финальная проверка `task-20260723T091854Z-final-read-only-review-dark-orange-instrument-profile` также завершилась без P0/P1/P2.
- Перед установкой сохранена прежняя согласованная пара: `<PRIVATE_APP_BACKUP>`, SHA-256 `8046e3804b3d1f5f741e81c7e821894dffd618941cad6792742da4918ef31fd4`; `<PRIVATE_ASSETS_BACKUP>`, SHA-256 `871b1b74b98741c48c34d178588ae816fdba2e8197af1a36c46a3667acf70729`.
- Новый профиль установлен на тестовый робот: `gosha.bin` записан только по `0x20000`, `generated_assets.bin` — только по `0x800000`. NVS, `otadata`, таблица разделов и загрузчик не изменялись.
- `verify_flash` подтвердил точное совпадение обоих разделов. Повторное окно UART продлено до 600 секунд без паники, сторожевого таймера и повторной загрузки; минимальная свободная память `66999` байт.
- После установки этой точной пары подтверждены свежие heartbeat-события робота, живой OTA-контракт и согласованный цикл Android/панели.
- Доказательство: `<PRIVATE_VALIDATION_EVIDENCE>/task-20260723T111058Z-read-only/live-validation/acceptance-evidence.json`, SHA-256 `600c3d526dddf95a39c38c1cf126a952d02a2e6b7b506f8a591b6f94d7690f0e`.
- Следующий шаг — операторская визуальная проверка глаз, Wi-Fi, батареи и центрального статуса, затем слово пробуждения и один голосовой диалог.
- Не записывать этот ресурсный образ на произвольную старую или другую прошивку: раздел также содержит модель и метаданные слова пробуждения текущего профиля.

## Предыдущая контрольная точка 2026-07-22

- Рабочее дерево: `<FIRMWARE_TRIANGLE_WORKTREE>`.
- Ветка: `feature/firmware-triangle-runtime`, база `origin/main` (`0980c2a`).
- Текущий этап: неблокирующий прошивочный источник общего контура «робот — приложение — панель» установлен на тестового робота и подтверждён в сети.
- Сетевые и автоматные обратные вызовы только ставят безопасное событие в ограниченную очередь; HTTP выполняет отдельная низкоприоритетная задача.
- Конфигурация маршрута и ключа приходит через уже проверенный OTA-ответ и не печатается в журнал.
- Первый живой образ вызвал `Interrupt WDT` из-за гонки уничтожения `HttpClient`/`EspTcp`; устройство сразу возвращено на стабильную прошивку.
- Исправление времени жизни TCP и TLS находится в `firmware/local_components/78__esp-ml307`, подключено через `override_path`; служебный HTTP использует отдельный `connect_id=4`.
- Финальная полная сборка `gosha-v1` прошла, а ИИ-офис `task-20260722T115308Z-final-review-firmware-tcp-tls-lifetime-fix` не нашёл P0/P1/P2.
- Исправленный образ установлен только в раздел приложения `0x20000`. За контрольное окно более 210 секунд не было паник, перезапусков или сторожевого таймера; события робота появились в живой базе платформы, `WSControl` продолжил проверочные обмены.
- Сквозной Android P1 повторно завершён `2026-07-23` уже после установки тёмно-оранжевой пары: робот, приложение и панель показали согласованные живые состояния. Голосовой диалог после финальной пары приложения и ресурсов отдельно вручную ещё не повторялся.

## Что это за проект

`GOSHA_FIRMWARE` — отдельный репозиторий собственной прошивки `Гоша`, который должен привести к независимому устройству с собственной платформой и собственным профилем платы.

## Как писать новые записи

- Все новые планы, отчёты, объяснения и контрольные точки оформляй русским техническим языком по правилам из `../AGENTS.md`.
- Не используй необъяснённый английский жаргон в обычном тексте.
- Английский оставляй только для команд, путей, имён файлов, веток, коммитов и кода.

## Текущая точка

- В `GOSHA_FIRMWARE` уже унифицированы правила общения агента:
  - корневой и локальные `AGENTS.md` требуют понятный русский технический язык;
  - для типовых терминов уже закреплены русские формы и пояснения.
- Для параллельной ручной разработки теперь отдельно зафиксирована политика `git worktree`:
  - ручные рабочие деревья выносятся в `<TASK_WORKTREE>`
  - для длительных аппаратных и акустических задач используются отдельные ветки `lab/*`
- Репозиторий уже создан как отдельный локальный контур.
- Для репозитория уже настроен удалённый `origin`:
  - `git@github.com:MaxCorpOrg/GOSHA_FIRMWARE.git`
- Ветка `main` уже отправлена в GitHub и отслеживает:
  - `origin/main`
- Публичный адрес репозитория:
  - `https://github.com/MaxCorpOrg/GOSHA_FIRMWARE`
- Общая карта связанных контуров:
  - `<PLATFORM_WORKSPACE>/docs/GOSHA_PROJECT_MAP_RU.md`
- Каноническая исходная база для первого импорта:
  - `<LEGACY_FIRMWARE_SOURCE>`
- Зафиксирована версия этой базы:
  - `2.2.2`
- Канонический исходный профиль первой версии:
  - `otto-robot`
- Новый целевой профиль:
  - `gosha-v1`
- Текущий реально прошитый порог слова пробуждения:
  - `38`
- Ближайшее решение по wake word:
  - переход на `50` пока не нужен
- Исходная база уже импортирована в:
  - `<FIRMWARE_WORKSPACE>/firmware`
- Профиль `gosha-v1` уже создан как копия `otto-robot` и подключён в `CMake` и `Kconfig`.
- Профиль `gosha-v1` уже виден в `scripts/release.py --list-boards`.
- Локальная среда сборки уже поднята:
  - `ESP-IDF 5.5.2`
  - путь: `<ESP_IDF_ROOT>`
- Первый полный build уже выполнен успешно.
- Ключевые артефакты уже получены:
  - `firmware/build/merged-binary.bin`
  - `firmware/build/gosha.bin`
  - `firmware/releases/v2.2.2_gosha-v1.zip`
- Первое тестовое устройство уже прошито.
- На реальном устройстве уже подтверждено:
  - профиль `gosha-v1` загружается;
  - вариант платы определяется как `non-camera`;
  - экран больше не падает на ранней инициализации;
  - прошивка доходит до режима настройки `Wi‑Fi` и поднимает точку доступа с веб-страницей настройки;
  - локальный сервер управления `WebSocket` поднимается на `8080`.
- Исправлен следующий реальный дефект подключения:
  - прошивка была собрана со старым OTA/config-маршрутом на порту `8876`
  - этот старый адрес отдавал `404`
  - адрес по умолчанию переключён на логический маршрут `PRIMARY_PLATFORM_SERVER:18876/gosha/ota/`
  - исправленный образ уже собран и прошит на тестовое устройство.
- Выполнен первый большой продуктовый перевод пользовательского слоя прошивки:
  - язык по умолчанию переключён на русский
  - обновлены русские строки экрана
  - имя точки доступа переведено на схему `GOSHA-A-<хвост MAC>`
  - имя настройки по Bluetooth переведено на `GOSHA-Setup`
  - имя платы для пользователя переведено на `GOSHA`
  - имя приложения в сборке переведено на `gosha`
- Для `gosha-v1` включён режим собственного слова пробуждения:
  - `Custom Wake Word`
  - внутренний токен `gosha`
  - пользовательское отображение `Гоша`
- Для уменьшения ложных пробуждений подготовлена следующая правка профиля:
  - `CONFIG_CUSTOM_WAKE_WORD_THRESHOLD`
  - было `20`
  - затем стало `35`
  - затем стало `45`
  - затем стало `40`
  - текущий подтверждённый живой шаг: `38`
  - это следует из контракта `Kconfig`: меньшее число означает более чувствительный режим
- Важно:
  - `gosha-v1` не входит в список плат, где доступен `CONFIG_USE_DEVICE_AEC`
  - значит часть самопроизвольных срабатываний может идти не только от порога, но и от акустического эха собственного динамика
- Уже подтверждено на живом устройстве:
  - переход `20 -> 35` заметно уменьшил ложные пробуждения
  - на `45` отклик на имя стал слишком слабым
  - на `40` отклик вернулся, но всё ещё оставался слабым
  - при громкой речи без имени `Гоша` лишние срабатывания ещё встречаются
  - образ с порогом `38` уже собран канонически через `scripts/release.py`, прошит и подтверждён по boot log строкой `set det threshold to 0.380000`
  - важно помнить: обычный `idf.py build` не гарантирует применение `sdkconfig_append` из `boards/gosha-v1/config.json`
- На реальном устройстве уже подтверждено:
  - подключение к домашнему `Wi‑Fi`
  - успешная привязка к панели
  - срабатывание слова пробуждения `Гоша`
  - открытие голосового `WebSocket`
  - распознавание русской фразы `Привет`
  - голосовой ответ робота на русском
- Выполнен отдельный аудит пользовательских китайских строк в активной плате `gosha-v1`.
- Переведены на русский:
  - описания `MCP`-инструментов
  - описание `press-to-talk`
  - runtime-логи
  - сообщения ошибок и статусы платы
- Дополнительно переведён общий тайм-аут сетевого подключения в `boards/common/nt26_board.cc`.
- Важно:
  - китайские символы ещё встречаются в комментариях исходников и неактивных частях дерева;
  - текущая пользовательская цель закрыта именно для активного слоя `gosha-v1`, а не для всего исторического импорта целиком.
- Текущий серверный голос работает через `EdgeTTS`, поэтому качество и набор тембров частично ограничены платформой, а не только прошивкой.
- Совместимый путь `WebSocket` пока сознательно оставлен как `/xiaozhi/v1/`, чтобы не ломать текущий серверный голосовой контур.
- Новая текущая живая работа в `main`:
  - исправлен дефект локальной голосовой подсказки при входе в режим настройки `Wi‑Fi`;
  - причина локализована в `AudioService`: локальные `Opus`-пакеты по `80 ms` ошибочно подавались в декодер как `60 ms`;
  - в `firmware/main/audio/audio_service.cc` добавлен расчёт реальной длительности `Opus`-пакета по `TOC`;
  - обновлённый `gosha.bin` уже собран и прошит на тестовое устройство через `<ROBOT_SERIAL_PORT>`
- Исправленный класс:
  - `firmware/main/boards/gosha-v1/otto_emoji_display.*`
  - тот же дефект одновременно устранён и в `otto-robot`, чтобы не держать расхождение в базовом профиле.
- Зафиксированы предварительные аппаратные документы:
  - `HARDWARE_MANIFEST_RU.md`
  - `PIN_MAP_RU.md`
- Репозиторий уже содержит импортированное дерево прошивки и подтверждённый первый результат сборки.

## Главные ограничения

- Не превращать `AI_ROBOT` в живую зависимость нового репозитория.
- Не хранить build-артефакты и локальные `sdkconfig` в git.
- Не делать рискованные аппаратные изменения до подтверждения манифеста и pin map.
- Не смешивать этот репозиторий с `GOSHA_PLATFORM`.

## Ближайший приоритет

- На новом устройстве не запускать движения: неисправный сервопривод левой руки должен оставаться отключённым до замены.
- Без движения проверить экран, оранжевые глаза, звук, микрофон, настройку Wi-Fi, статус, батарею и события платформы.
- После замены проверить новую серву без качалки, выполнить `Home`, правильно установить механику и провести только малый тест штатным управляющим путём.
- Прогнать несколько подряд срабатываний слова пробуждения `Гоша`.
- На уже прошитом пороге `38` оценить баланс между уверенностью отклика и ложными пробуждениями по сравнению с `20`, `35`, `40` и `45`.
- Прогнать несколько подряд коротких русских вопросов и ответов.
- Повторно включить режим настройки `Wi‑Fi` и ушами подтвердить, что голосовая подсказка больше не срывается в шум и не замолкает.
- Если остаточный дефект по звуку сохранится, следующим шагом разбирать ресемплинг `16000 -> 24000` и выходной тракт `NoAudioCodecSimplex`.
- Проверить, влияет ли несовпадение частоты `16000 -> 24000` на качество звучания.
- Не выполнять живые raw-проверки локального `:8080` до замены левой сервы. При необходимости фиксировать только список разрешённых read-only команд без их прямого запуска на роботе: `gosha.identity.get`, `self.otto.get_status`, `self.battery.get_level`, `self.otto.get_ip`.
- Разобрать локальный портал настройки в сети точки доступа устройства, который в одном из прогонов открывался пустой страницей.
- Отдельно фиксировать, что часть жалоб на тембр голоса будет закрываться не в этом репозитории, а в `GOSHA_PLATFORM` через новый движок синтеза речи.
- Проверить спорные выводы `PWM` для сервоприводов и подсветки, которые журнал пометил предупреждениями `ledc`.
- После замены левой сервы и отдельной механической приёмки прогнать расширенный аппаратный цикл:
  - голос
  - OTA
  - MCP
  - `status`
  - `battery`
  - одно безопасное действие
- Затем уже отдельно решать, нужен ли новый голосовой псевдоним `/gosha/v1/` поверх текущего совместимого `/xiaozhi/v1/`.
- Если понадобится следующий санитарный пакет, отдельно дочистить:
  - китайские комментарии
  - неактивные `zh-*` ресурсы
  не вмешиваясь в уже подтверждённый живой пользовательский слой.
- Если позже появятся новые локальные `AGENTS.md`, переносить в них тот же блок правил русского технического языка.
